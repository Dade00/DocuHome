<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">#33073578</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />

    <style name="SpinnerTheme" parent="android:Widget.Material.Spinner">
        <item name="android:background">@drawable/itemarrows</item>
    </style>

    <style name="loadingTh" parent="android:Widget.Material.ProgressBar">
        <item name="colorAccent">#0800FF</item>
    </style>

    <style name="IMGtheme" parent="android:Widget.Material.Spinner">
        <item name="android:background">@drawable/strockeimg</item>
    </style>
</resources>
